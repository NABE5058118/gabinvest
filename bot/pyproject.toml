[project]
name = "gab-invest-bot"
version = "1.0.0"
description = "GAB Invest Telegram Bot"
requires-python = ">=3.12"
dependencies = [
    "aiogram>=3.4,<4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
